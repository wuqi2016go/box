<view class="i-class i-avatar i-avatar-{{ shape }} i-avatar-{{ size }} {{ src ? 'i-avatar-image' : '' }}">
    <image src="{{ src }}" wx:if="{{ src !== '' }}" class="{{ iimage }}" ></image>
    <view class="i-avatar-string" wx:else><slot></slot></view>
</view>
